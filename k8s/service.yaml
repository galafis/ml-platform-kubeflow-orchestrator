apiVersion: v1
kind: Service
metadata:
  name: ml-platform-api
  namespace: ml-platform
  labels:
    app: ml-platform-api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: ml-platform-api
